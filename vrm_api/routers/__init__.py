from __future__ import annotations
"""Route modules for `vrm_api`. Each is a thin, narrow-verb wrapper — see
`main.py`'s module docstring for the trust-boundary rules every route here
must keep true."""
