"""Victron Monitor — Python side.

Node-RED flows, Apps Script and SQL for this product live in `victron-monitor/`
at the repo root. The Python modules live here so they sit on the Streamlit
app's import path alongside `calculations/` and `proposals/`.
"""
